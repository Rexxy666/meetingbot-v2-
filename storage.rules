rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 頭像：公開讀取；僅登入者可寫入自己的資料夾
    match /avatars/{userId}/{fileName} {
      allow read: if true;
      allow write: if request.auth != null
        && request.resource.size < 2.5 * 1024 * 1024
        && request.resource.contentType.matches('image/.*');
    }
  }
}
